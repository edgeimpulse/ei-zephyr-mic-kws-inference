sample:
  description: Run inference using one of the ST sensor hat
    application
  name: ei-zephyr-imu-inference
common:
    tags: introduction
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Hello World! (.*)"
